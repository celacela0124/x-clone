// このファイルを js/config.js にコピーして、実際の値を入力してください
// config.js はリポジトリにコミットする(GitHub Pagesで配信するため)。
// anon key は RLS で保護される公開前提の値。service role(管理者)キーは絶対に書かないこと。
const SUPABASE_URL = 'https://your-project-ref.supabase.co';
const SUPABASE_ANON_KEY = 'your-anon-key-here';
